---
description: Answer using persistent memory and verified live-project evidence.
agent: coordinator
subtask: false
---

Answer this question through the memory-aware ask workflow: $ARGUMENTS

Ask archivist to consult `index.md` first and return relevant memory with provenance. Verify mutable claims against the live checkout. Perform focused reads directly and delegate broad multi-file investigation to coder. Live project evidence controls when it conflicts with memory.

If memory and project evidence are insufficient, obtain operator approval before external research. In the final response distinguish memory evidence, current-project evidence, external evidence, contradictions, and unknowns.

Ask archivist to append an ask log with timestamp, normalized topic, evidence categories, and pages consulted, without copying the full conversation. Persist the answer itself only if it meets durable-memory criteria.
